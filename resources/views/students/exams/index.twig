{% from 'components/core/link-card' import link %}
{% from 'components/core/breadcrumbs' import breadcrumb %}
{% from 'components/core/show-header' import show_header %}


{% extends 'components/core/student-layout' %}

{% block content %}
{{ breadcrumb() }}

<div class="flex flex-col max-w-full overflow-auto mx-8 rounded-xl bg-white p-4 shadow-sm ring-1 ring-gray-950/5">
    <div class="flex justify-between px-2 py-3 border-b-1 border-spacing-2 border-gray-200">
        <div class="flex text-3xl font-semibold text-blue-900">
            My Exam
        </div>
        <div class="flex">
            <button class="bg-blue-900 text-gray-200 hover:bg-blue-600 hover:text-white duration-300 rounded-lg p-2 font-semibold">Add Exam</button>
        </div>
    </div>
    
    <div class="flex flex-col my-2 w-full p-4 gap-5 justify-center">
        <div class="flex self-center gap-5">
            {% include 'components/core/exam-card-student' %}
            {% include 'components/core/exam-card-student' %}
            {% include 'components/core/exam-card-student' %}
        </div>
        <div class="flex self-center gap-5">
            {% include 'components/core/exam-card-student' %}
            {% include 'components/core/exam-card-student' %}
            {% include 'components/core/exam-card-student' %}
        </div>
    </div>
    
    
</div>

{% endblock %}