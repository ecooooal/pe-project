{% import 'components/core/forms' as forms %}

<div id="access-code-form" class="flex flex-col gap-y-4 w-full">
    {{ forms.form_field(
        label:forms.label(
            id:'access-code', 
            content:'Access code', 
            attributes:{
                'class':'whitespace-nowrap text-sm/6 font-medium text-gray-900  after:text-red-500 after:content-[\'\*\']'}
        ),
        input:forms.text(
            name:'access-code', 
            value: old['access-code'],
            attributes:{
                'class':'block w-full rounded-md bg-white px-3 py-1.5 text-base text-gray-900 outline-1 -outline-offset-1 outline-gray-300 placeholder:text-gray-400 focus:outline-2 focus:-outline-offset-2 focus:outline-indigo-600 sm:text-sm/6',
                'required': true}
        ),
        errors: errors['access-code'],
        isColumn: true
    )}}
</div>