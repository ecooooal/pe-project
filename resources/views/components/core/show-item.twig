{% macro show_item(label, value, label_attributes, value_attributes) %}
    <div class="flex gap-x-2">
        <span class="{{ label_attributes ? label_attributes ~ ' font-medium' : 'font-medium' }}">
            {{ label }} : 
        </span>
        <p class="{{ value_attributes }}">
            {{ value }}
        </p>
    </div>
{% endmacro %}