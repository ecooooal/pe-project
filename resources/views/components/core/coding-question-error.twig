<div id="name-points-form-error" class="flex gap-x-4 justify-between">
    <span class="text-red-500 text-sm font-semibold whitespace-nowrap">{{ errors.get('name')[0] }}</span>  
    <span class="text-red-500 text-sm font-semibold whitespace-nowrap">{{ errors.get('points')[0] }}</span>  
</div>

<div id="instruction-form-error">
     <span class="text-red-500 text-sm font-semibold whitespace-nowrap">{{ errors.get('instruction')[0] }}</span>  
</div>


<div id="language-form-error">
     <span class="text-red-500 text-sm font-semibold whitespace-nowrap mt-1">{{ errors.get('supported_languages')[0] }}</span>  
</div>

